// const getTable = (num) => {
//   for (i = 0; i <= 10; i++) {
//     console.log(`${num} X ${i} =${i * num}`);
//   }
// };
// getTable(13);
const Table = (num) => {
  for (i = 0; i <= 100; i++) {
    console.log(`${num} X ${i} =${i * num}`);
  }
};
Table(1);
