const obj = {
  name: "SK",
  city: "Nanded",
  contact: 9696996,
};
const valuesArr = Object.entries(obj);
console.log(valuesArr);

// const keys = Object.keys(obj);
// const newArr = keys.map((keys) => obj[keys]);
// console.log(newArr);
